pub mod tilemap{
    use bevy::{
        prelude::*,
    };

    #[derive( Component, Reflect)]
    struct Tilemap{

    }

    pub fn new( ) -> TileMap{
        TileMap{
            
        }
    }
}